# RTX 3060 (Ampere, 12 GB VRAM) — PyTorch with CUDA 12.4
#
# Requires an NVIDIA driver that supports CUDA 12.4+ (typically driver >= 550).
# EfficientNetV2-L inference/attacks fit easily in 12 GB.
#
# Install (from my_work/):
#   pip uninstall torch torchvision -y
#   pip install -r requirements.txt
#
# Verify GPU:
#   python -c "import torch; print(torch.cuda.is_available(), torch.cuda.get_device_name(0))"

--extra-index-url https://download.pytorch.org/whl/cu124

requests>=2.31.0
numpy>=1.26.0
Pillow>=10.0.0
datasets>=2.19.0
torch==2.6.0
torchvision==0.21.0
kornia>=0.7.0
