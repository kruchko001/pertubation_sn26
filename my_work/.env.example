# Copy this file to .env and fill in your values:
#   cp .env.example .env        (Linux/macOS)
#   copy .env.example .env      (Windows)
#
# Used by gh/git on other machines (GitHub).

GITHUB_TOKEN=
GH_TOKEN=
